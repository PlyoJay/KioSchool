<UserControl x:Class="KioSchool.Controls.Cafe.DrinkSelectionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KioSchool.Controls.Cafe"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <ScrollViewer>
            <ScrollViewer.Resources>
                <Style TargetType="ScrollBar">
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="Width" Value="6"/>
                </Style>
            </ScrollViewer.Resources>
            <ItemsControl ItemsSource="{Binding DrinkList}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid x:Name="ugrdMenu" Columns="3"/>
                    </ItemsPanelTemplate >
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button HorizontalAlignment="Center" VerticalAlignment="Center"
                                Width="140" Height="150" Padding="0" Margin="0 15"
                                HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                                Command="{Binding DataContext.OpenOptionDialogCommand,
                                    RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding }">
                            <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30"/>
                                    <RowDefinition/>
                                    <RowDefinition Height="30"/>
                                </Grid.RowDefinitions>
                                <Label Content="{Binding Name}" HorizontalAlignment="Left" Margin="5 0"/>
                                <Image Grid.Row="1" Source="{Binding DrinkImage, Converter={StaticResource ImagePathConverter}}"/>
                                <Label Grid.Row="2" Content="{Binding Price, Converter={StaticResource PriceToStringConverter}}" HorizontalAlignment="Right" Margin="5 0"/>
                            </Grid>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
