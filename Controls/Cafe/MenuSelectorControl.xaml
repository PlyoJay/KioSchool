<UserControl x:Class="KioSchool.Controls.Cafe.MenuSelectorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KioSchool.Controls.Cafe"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="90"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Background="#FFE57D">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>
            <!--<materialDesign:PackIcon Kind="ChevronLeft" Height="90" Width="50"/>-->
            <Button x:Name="btnCategoryLeft" Height="90" Width="50" Background="Transparent" BorderThickness="0">
                <materialDesign:PackIcon Kind="ChevronLeft" Height="90" Width="50" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black"/>
            </Button>

            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Disabled">
                <StackPanel x:Name="spCategory">
                    
                </StackPanel>
            </ScrollViewer>

            <Button x:Name="btnCategoryRight" Grid.Column="2" Height="90" Width="50" Background="Transparent" BorderThickness="0">
                <materialDesign:PackIcon Kind="ChevronRight" Height="90" Width="50" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black"/>
            </Button>
        </Grid>
        <Grid Grid.Row="1">
            <ScrollViewer>
                <UniformGrid x:Name="ugrdMenuItems" Columns="4">

                </UniformGrid>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
