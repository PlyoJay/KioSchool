<UserControl x:Class="KioSchool.View.CafeKioskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KioSchool.View"
             xmlns:vm="clr-namespace:KioSchool.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid>
            <TextBlock Text="{Binding TrainingManager.CurrentStep.Instruction}"
                       FontSize="24" FontWeight="Bold" Foreground="Blue"
                       HorizontalAlignment="Center"/>
        </Grid>
        <Frame x:Name="CafeFrame" Grid.Row="1"
               NavigationUIVisibility="Hidden"/>
    </Grid>
</UserControl>
