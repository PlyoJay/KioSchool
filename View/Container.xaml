<UserControl x:Class="KioSchool.View.Container"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KioSchool.View"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <materialDesign:DialogHost DialogTheme="Inherit" Background="Transparent">
        <materialDesign:DrawerHost IsLeftDrawerOpen="{Binding IsDrawerOpen, Mode=TwoWay}" OpenMode="Default">
            <materialDesign:DrawerHost.LeftDrawerContent>
                <DockPanel MinWidth="130" Background="White">
                    <Grid Height="55" DockPanel.Dock="Top">
                        <ToggleButton Margin="10 0"
                            HorizontalAlignment="Right"                           
                            IsChecked="{Binding IsDrawerOpen, Mode=TwoWay}"
                            Style="{StaticResource MaterialDesignHamburgerToggleButton}" />
                    </Grid>
                    
                    <ListBox>
                        <ListBoxItem Name="lbxiHome" MouseLeftButtonUp="lbxi_MouseLeftButtonUp">
                            <StackPanel Orientation="Horizontal" Margin="10 0">
                                <materialDesign:PackIcon Kind="Home" Width="24" Height="24" VerticalAlignment="Center"/>
                                <TextBlock Text="홈으로 돌아가기" FontSize="16" FontWeight="Bold" Margin="10 0"/>
                            </StackPanel>
                        </ListBoxItem>

                        <Separator/>

                        <ListBoxItem Name="lbxiCafe" MouseLeftButtonUp="lbxi_MouseLeftButtonUp">
                            <StackPanel Orientation="Horizontal" Margin="10 0" >
                                <materialDesign:PackIcon Kind="LocalCafe" Width="24" Height="24" VerticalAlignment="Center"/>
                                <TextBlock Text="카페 주문하기" FontSize="16" FontWeight="Bold" Margin="10 0"/>
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem Name="lbxiHospital" MouseLeftButtonUp="lbxi_MouseLeftButtonUp">
                            <StackPanel Orientation="Horizontal" Margin="10 0" >
                                <materialDesign:PackIcon Kind="HospitalBuilding" Width="24" Height="24" VerticalAlignment="Center"/>
                                <TextBlock Text="병원 접수하기" FontSize="16" FontWeight="Bold" Margin="10 0"/>
                            </StackPanel>
                        </ListBoxItem>
                    </ListBox>
                </DockPanel>
            </materialDesign:DrawerHost.LeftDrawerContent>
            <Border>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="55"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <ToggleButton x:Name="MenuToggleButton" AutomationProperties.Name="HamburgerToggleButton" 
                                  IsChecked="{Binding IsDrawerOpen, Mode=TwoWay}"
                                  HorizontalAlignment="Left" Margin="10 0"
                                  Style="{StaticResource MaterialDesignHamburgerToggleButton}"/>
                </Grid>
            </Border>
        </materialDesign:DrawerHost>
    </materialDesign:DialogHost>
</UserControl>
